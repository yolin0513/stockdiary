# 三代理投票紀錄（2026-09-11）

- 模型：三位皆為 Fable 5.1（`claude-fable-5-1`），未撞到額度，未改用 Opus 5。
- 題目與事實表：規劃階段的 `vote-prompt.md`（內容等同 `FEASIBILITY.md` §1–§4 摘要）。
- 各評審只讀事實表與 TripQuest docs，未打任何外部網址。

| 題 | A | B | C | 決議 |
|---|---|---|---|---|
| Q1 上櫃資料 | C（Actions，先驗證；失敗退 B） | C（同） | C（同） | **C 3:0** |
| Q2 誰結算 | B | B（A 只當上市備援） | B（Cron＝Actions 非 Worker） | **B 3:0** |
| Q3 新聞 | A；udn (i) 不接 | A 但 Cron 改每日 4–5 次；udn (i) | A；udn (i)；注意 10 ms CPU | **A 3:0，udn 不接 3:0，Cron 每日 5 次** |
| Q4 損益模型 | A | A | A | **A 3:0** |
| Q5 模型 | A（Sonnet 5） | A | A | **A 3:0** |

## 附帶共識
- 不自動輪詢 mis（B、C）；只做手動更新。
- 不餵股數與損益金額給模型（B）。
- `web_search` 不進 v1（B：留作手動加值；C：不要）。
- 輸出結構化 JSON ＋ 禁用詞過濾 ＋ 不可關閉的免責標籤（三位）。
- 首次啟用 AI 需勾選同意（B、C）。

## 各評審提出的加值功能
- A：除權息「券商均價核對」、匯出 CSV／離線任意日重算、資料狀態列。
- B：今日事件提醒（除權息、停牌）、產業集中度、匯出／匯入 JSON。
- C：除權息日曆、我 vs 大盤、開休市日曆內建。

## 各評審列的「不要做」
- 券商 API／下單（A）；AI 評分／排行／健康度（A、B、C）；抓全文餵 AI（A）；盤中自動輪詢（B、C）；`web_search` 自己找新聞（C）；帳號與雲端同步（B）。

## 各評審列的最大風險
1. mis.twse 非官方，隨時失效 → 整段可降級（三位）。
2. 櫃買封鎖面擴大到 GitHub Actions IP → 退非官方 Yahoo、除權息手動（三位）。
3. AI 觀察被家人當建議 → 禁用詞過濾＋不可關閉標籤＋不餵金額（三位）。
