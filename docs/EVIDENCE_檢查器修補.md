# 檢查器修補的三段證據（EVIDENCE_檢查器修補.md）

> 四家統一檔名（Dispatch 2026-09-24 補充說明（四）第 2 點）。`docs/STATUS.md` 是接手契約，只留摘要並指向這裡；逐件的三段證據寫在這裡，驗收時一次讀完、以後也查得到。
> 對話回報會丟，§5.7 也寫明證據只存在本機等於沒有紀錄——所以證據進 repo。

## 共通做法（每一件都照這個做，下面不再重複）

- **在哪裡跑**：repo 外的暫存複本，commit 之後跑；不碰真的 repo。打網路的一律換成本機假伺服器或錄好的回應（`livecheck` 的「對真的證交所實跑」除外，那是它平常的用法，也在複本裡跑）。
- **修正前怎麼取**：一律從**明確的 commit** 取（逐件寫是哪一個），取出後先斷言**新舊檔案雜湊不同**、而且**舊版裡確實還有舊寫法**（逐件寫檢查了什麼）；不過就中止、不讀輸出。S3–S7 當時寫的是「從 GitHub 追蹤分支取」，當時追蹤分支所指的 commit 已在輸出裡印出，這裡換成明確的雜湊。
- **讀到的輸出是這一次的**：每次跑之前刪掉舊輸出檔；改壞的那一份有沒有被執行到，比雜湊（§5.11 第三層）。
- **擋下的理由要落在錯誤訊息的位置**（補充說明（四）第 1 點）：不數「整份輸出有沒有出現那個字串」——正常輸出本來就可能印出同一個字串。每一個比對理由的地方都另有兩個方向的對照組（只出現在別處 → 不算；出現在錯誤訊息裡 → 算）。
- **比對兩份結果之前，先斷言兩邊都抽到預期的數量**（第 3 點）：「相同」在母體是空的時候恆真。
- **突變的判定**（2026-09-24 起）：要「**只紅對應的那一種**」——紅在 `expect` 之外還有別組一起紅，判「多紅了別組」；本來就會連帶紅的要用 `alsoRed` 明列、`alsoRedWhy` 寫理由（`checkmutations` 擋沒寫的）。

## SPEC_檢查器修補 S3–S9

**S3 `assertaudit` 判斷邏輯的必敗對照組（`4e74c86`）**
- 修正前：`assertaudit.mjs` 取自 `84e3999`（檔案雜湊 `c5e233b`≠新版 `db81ffd`；舊版沒有 `auditjudge`、篩選 `r.n <= 2` 寫在本檔）。三種壞法打在舊版上：「母體 ≤ 2」改成挑不到 → 回 0、報告寫「（沒有）」；紅了的測試判成通過 → 回 0、印 ✓；崩掉的不判「沒收到資料」→ 兩支（`calctest` 崩、`calcviewtest` 正常）時回 0、印「收到 0 筆資料」（只過濾一支時是 P0 的「資料檔一筆都沒有」碰巧擋下，那一格不算，改用兩支重做）。
- 修正後：同樣三種壞法打在 `auditjudge.mjs` → 都回 1、印「對照組沒過」並點名是哪一組（二／三／一）。
- 突變（逐條實測紅了哪幾組）：四條各只紅自己那一組（對照一／二／三／四）。另：把「對照組沒過就停」拿掉 → 回 0。

**S4 `sweep` 的合成對照組（`cb81089`）**
- 修正前：`sweep.mjs` 取自 `84e3999`（雜湊 `5e503fd`≠新版 `bfdf086`；舊版沒有 `sweepjudge`、有 `eq(liveSw, LOCAL_VERSION,`）。版本比對改成恆真 → 對線上跑 20 項全過、回 0。
- 修正後：同一種壞法打在 `sweepjudge` → 停在對照組、回 1、沒有打網路，點名對照一、三。
- 突變：六條。五條各只紅自己那一組；「sw.js 比對永遠成立」紅對照一與對照三（兩組都靠同一行比對，已用 alsoRed 明列）。拿掉「對照組沒過就停」→ 20 項全過。

**S5 兩份清單的孤兒檢查（`e8245a3`）**
- 修正前：`assertaudit`、`auditjudge`、`gatescan`、`taptest` 取自 `cb81089`（四支雜湊都不同；舊版沒有 `ORPHAN_SKIP`／`auditOrphans`／`SD_AUDIT: ''`）。丟一支沒登記、帶 `| tail -1` 的 `scripts/ship.sh` → 舊 `gatescan` 回 0；`npm test` 鏈多一支 `zzqtest`、清單沒收 → 舊 `assertaudit` 回 0、報告沒提到它；舊 `taptest` 在稽核下把探針的空母體寫進報告（「來自 2 支測試」、多一筆 n=0）。
- 修正後：`gatescan` 回 1 點名 `scripts/ship.sh`；`assertaudit` 回 1 點名 `zzqtest`、不產報告；`controltest`（每版）回 1 點名 `zzqtest`；`taptest` 報告只剩 1 支、沒有 n=0。
- 沒有少挑：舊清單 29 支、新清單 32 支，少 0、多出正是 `taptest`、`controltest`、`gatescan`。
- 突變：九條。六條各只紅自己那一組；三條會連帶紅別組，已用 alsoRed 明列並寫理由：「取鏈上名稱時漏掉帶連字號的」連帶紅「對照六」與「assertaudit 過期」（`secret-leak-test` 取不到，同一個錯在三處都看得到）；「不看腳本內容」連帶紅「gatescan 理由過期」（`gatescan.mjs` 只靠內容被認出）；「走訪時沒跳過 node_modules」在本機連帶紅真實的孤兒檢查（本機的 node_modules 裡真的有檔名帶 push 的檔）。

**S6 `livecheck` 用錄好的回應當合成對照（`6734a22`）**
- 修正前：`livecheck.mjs` 取自 `cb81089`（雜湊 `1d79c52`≠新版 `37d03df`；舊版沒有 `livejudge`）。本機假證交所送錄好的回應、但拿掉 CORS 標頭，舊版的 CORS 比對改成恆真 → 那條印 ✓（另外兩條沒改的照常紅）。
- 修正後：同一種壞法打在 `livejudge` → 停在對照組、回 1，**假證交所收到 0 個請求**。拿掉「對照組沒過就停」→ 又打出 6 個請求、CORS 三條都印 ✓。
- 突變：九條，各只紅自己那一組。

**S7 閘門第零關（驗法登記）與驗法可換順序（`31cb4a9`）**
- 修正前：`gatepush.sh` 取自 `cb81089`（雜湊 `90a0b7f`≠新版；舊版沒有「第零關」）。用新驗法跑 → 情境 10、11 不符（回 0、假遠端被推動）。
- 修正後：15 種全部符合；預設順序與倒序各跑一次，15 種結論逐行相同（兩邊都斷言剛好 15 行；第一次用 grep 的字元類別抽到 0 行、「相同」恆真，已改掉並記下）。
- 突變（當時在複本裡手做）：拿掉整個第零關 → 只有 10、11 不符；只看登記檔在不在 → 只有 10 不符。三次都先放一份舊登記，確認驗法沒全過之後它被刪掉。
- **統籌者驗收指出的缺口**：這兩條突變與「驗法沒全過要刪掉登記」只在我的暫存腳本裡，repo 裡沒有；統籌者補跑「登記的雜湊對不上時不擋」，只有情境 10 紅、那個 commit 真的推了出去。→ 補在 `scripts/gateselftest.mjs`（見下）。

**S8 三支 build 只盤點（`77b026d`）**：結果見本檔最後「從 STATUS 搬過來的證據」；後來修成 F8（見下）。

**S9 補齊盤點表（`73d00d0`）**：每一格的實測寫在 STATUS「2026-09-24 檢查器盤點」那張 v9 盤點表（標「S9 補測」；表本身是 v9 的證據，留在 STATUS）；刪檔類格子都先確認檔案原本在（有內容、有追蹤）。這一件只盤點，沒有修正前／修正後。

## 驗收補件（2026-09-24，統籌者驗收指出的缺口）

**判定器：突變要「只紅對應的那一種」（`04b205e`；統籌者驗收指出）**
- 修正前：`mutjudge.judge` 只要**有一條**失敗訊息含 `expect` 就判「紅對了」，別組有沒有一起紅不看——所以 S3–S7 回報的「只紅對應那一種」，靠的是我另外手做的逐條工具，不是判定器本身。
- 修正後：紅在 `expect` 之外還有別組一起紅，判「**多紅了別組**」（不合格，列出多紅的那幾條）。本來就會連帶紅的，突變上用 `alsoRed` 明列那幾組的固定標籤，**而且必須寫 `alsoRedWhy`**（`checkmutations` 擋沒寫理由的、擋對不到測試原始碼的 `alsoRed`）。`checkmutations` 的對照：同時紅兩組 → 判多紅；`alsoRed` 列對 → 合格；列錯標籤 → 照樣判多紅；`alsoRed` 打錯字、空字串、沒寫理由 → 都報出來。
- **舊判定看不到的量**：用新判定把當時全部 92 條帶 `expect` 的突變重跑（`--expect-only`），**0 條沒紅、0 條紅錯地方、27 條多紅了別組**（BG 那 10 條另計，當時的 `buildtest` 已改成矩陣版）。逐條重跑取出完整的多紅清單、逐條看過，都是同一個錯在別處也看得到的連帶（例：成本說明整段拿掉，說明裡每一句各自的斷言一起紅；指向 `checkmutations` 的突變都會讓它把這條突變本身判過期），已逐條寫 `alsoRed`＋理由。
- 突變：以 `判定：` 開頭的四條（多紅也判紅、`alsoRed` 被忽略、`alsoRed` 打錯字也放行、`alsoRed` 不寫理由也放行），`A：判定忽略 expect` 的錨點跟著新程式更新。
- 名稱前綴撞號：新突變本來取名 `A3：`，跟既有 `uikittest` 的 `A3：` 撞在一起，`--only 'A3：'` 挑到別人的——改名 `判定：`。

**S7 補件：驗法的自我測試（`04b205e`，`scripts/gateselftest.mjs`，`npm run gateselftest`，約 7 分鐘）**
- 在暫存複本裡用 repo **工作區**的閘門檔（這一輪起五支：多了 `gatereason.mjs`）（`mutationtest` 改壞工作區的檔時它才看得到），逐種變體：改壞一處並 commit → 先放一份舊登記 → 跑 `gatetest.sh` → **逐行解析**十五種情境的結論（**斷言剛好 15 種**；解析程式自己有對照）→ 比對「不符的集合」**剛好等於**預期（多一種、少一種都算不符）→ 確認驗法跑到的是改壞的那一份（雜湊）→ 看登記檔。
- 結果（33 項全過）：原樣 → 不符 0、登記換成這一次的；原樣倒序 → 同上，**十五種結論逐一相同**；G1 拿掉整個第零關 → 剛好 10、11；**G2 登記的雜湊對不上時不擋（統籌者補跑的那一種）→ 剛好 10**；G3 沒有登記檔時不擋 → 剛好 11；三種突變都確認**驗法沒全過時，先放的舊登記被刪掉**。
- 驗法本身的突變登記在 `mutationtest`（指向 `gateselftest`）：GV「一開跑不刪舊登記」、GV「沒全過也寫登記」——一改壞，「驗法沒全過，先放的舊登記要被刪掉」那一條就紅。
- 統籌者自己也踩過同型的：第一次用 grep 抽 ✓／✗ 兩邊都抽到 0 種、「相同」恆真。**「相同」在母體是空的時候恆真**——這支自我測試每一次都另外斷言剛好 15 種。

**F8：三支 build 資料變少照樣寫檔（`3cf33ae` 第一版 → `04b205e` 第二版 → 這一輪第三版：補充說明（四）第 5 點）**

> 這是財務資料：`build-dividends` 在一筆都收不進來時，會寫出「0 檔」的 `dividends.json`，App 照常顯示、沒有任何提示。

- **修法**：寫任何檔之前先驗完每一個單位（來源不是陣列、0 筆、欄位逐筆對不上、解析不了、取不到、`build-dividends` 收進來 0 檔、比上一次成功的少一半以上、上一次的輸出壞掉）；有任何一項不過就一個檔都不寫，**每一條理由以單位開頭**。寫檔：先寫 `<輸出檔>.tmp` 再換上；**只清自己寫出的暫存檔**——暫存檔的位置被別的東西佔住（例如同名資料夾）就不動它、照實講；**清理本身失敗也點名留下了哪個檔、不中斷**；這些設計好的停下只印訊息、不印堆疊（沒料到的例外才印）。JLPT、MealMate 都中過的 `ERR_FS_EISDIR`（去刪那個同名資料夾、刪不掉、再丟一次例外、吐堆疊）就是這一格。
- **驗法**（F8 四家統一、照 MealMate 的形狀）：`scripts/buildtest.mjs` 的母體是**逐字寫出的「單位 × 情境」矩陣**，共 **54 格**：日曆（休市日公告）× 5、股利（t187ap45_L）× 5、代號表 6 份來源 × 4 ＋ 3 個市場 × 變少、日曆的「沒有這一年」「新的一年變少」、股利的「後面幾筆改名」「代號全不合格式」「現金股利全空」、三支「上一次的輸出壞掉」各 1、三支「寫檔失敗」各 3（暫存檔的位置被資料夾佔住、輸出檔設成唯讀而換不上去、換不上去而且清理也失敗）。前置斷言確認這幾張表＝單位 × 情境、來源＝腳本裡的 `SOURCES`。
  每一格四件事：**回傳非 0**、**理由點名這個單位與這一種狀況**（只在「不寫檔：」或「寫檔失敗：」後面的「  · 」清單裡找，狀況字眼對邊界——「0 筆」湊不到「10 筆」）、**`data/` 整個目錄的雜湊前後相同**（「清理也失敗」那格例外：暫存檔本來就會留下，改驗「輸出檔沒被蓋掉、留下的暫存檔有點名」）、**不吐堆疊**。分三類統計：擋／碰巧擋下（回非 0、輸出沒變，但理由沒點名這個單位或吐了堆疊）／沒擋。每一支另有「正常輸入要寫檔、回 0」的對照。擷取理由、目錄雜湊、堆疊判斷、字眼邊界，各自有兩個方向的對照。
- **修正前**（矩陣版 `buildtest` 配舊版的三支 build：先斷言三支的雜湊與新版不同、舊版含舊寫法；舊版有 `buildguard.mjs` 的就用它自己那一份，不然舊的 build 會用到新的寫檔函式）：

  | 版本 | 原始（54 格） | 不算數的格 | 算數的格：擋／碰巧擋下／沒擋 |
  |---|---|---|---|
  | `77b026d`（還沒有任何關卡） | 擋 0、碰巧擋下 33、沒擋 21 | 6 | 48 格：**0／30／18** |
  | `3cf33ae`（第一版） | 擋 5、碰巧擋下 43、沒擋 6 | 3 | 51 格：**5／43／3** |
  | `04b205e`（第二版） | 擋 9、碰巧擋下 39、沒擋 6 | 3 | 51 格：**9／39／3** |
  | 這一輪（第三版） | **擋 54、碰巧擋下 0、沒擋 0** | — | 54 格全擋 |

  **不算數的格子**（照 MealMate 的做法，不把對自己有利的格子算進去）：「暫存檔的位置被資料夾佔住」三格——三個舊版的暫存檔都不在 `<輸出檔>.tmp` 那個位置（`77b026d` 根本不寫暫存檔，後兩版的檔名帶程序編號），照判準它們在這格「沒擋」，但那不代表舊版在那裡有洞，所以扣掉。`77b026d` 另外扣「清理也失敗」三格：它不寫暫存檔，沒有東西要清。
  算數的格子裡：`77b026d` 的「碰巧擋下」多是被別的規則擋下、或只丟出沒點名單位的例外（例：日曆空陣列只報「公告裡沒有這一年」；缺檔丟 ENOENT 堆疊）；`3cf33ae` 與 `04b205e` 的日曆、代號表各格理由其實有點名，但**失敗時吐了堆疊**（`console.error(e)`、`e.stack`），依第 5 點算碰巧擋下；三個舊版的「換不上去」都**把暫存檔留在輸出目錄**（沒擋）。
  **這張表最該看的一格是 `77b026d` 的「擋 0」**：算數的 48 格裡，真正有意擋下的是 0 格，碰巧擋下 30 格。**如果只看回傳值，會得到「48 格裡擋了 30 格」的結論，看起來還不錯**；實際上沒有一格是它自己認得出來而擋的。這就是「被別的規則碰巧擋下，一律不算擋」這條判準的價值。
- **突變**（`--only 'BG：'`、`--only 'WF：'`，新判定「只紅對應的那一種」）：`BG：` 25 條（每一道關卡、每一個包裝各一條；以前寫 26 條是把一行註解也數進去了，2026-09-24 更正）＋ `WF：` 4 條（換不上去時不清暫存檔、清理失敗時中斷、暫存檔的位置被佔住時連別人的東西一起刪、設計好的停下也印堆疊）＋ `RS：狀況字眼不對邊界`，全部紅在指定的格子；會連帶紅的用 `alsoRed` 明列、`alsoRedWhy` 寫理由。等價突變一條（「股利取不到時直接拋錯」：外層的 try 接得住，§5.12），換成「完全不看 HTTP 狀態」。
- **§5.14 真實資料**：`04b205e` 在暫存複本對真的來源各跑一次（官方 OpenAPI），三支都沒被誤擋（日曆 2026 年 243 個交易日、平日休市 18 天；代號表 2768 檔），沒留下暫存檔。這一輪只改了寫檔與訊息的格式，關卡的判準沒變。
- **新版對真實資料的第一次實跑**（2026-09-24，在 repo 裡直接重跑 `build-stocks` 更新出貨的代號表，v0.7.24）：照常放行、回 0，沒留下暫存檔。前後逐檔比對：總數 2768 → 2768，興櫃 363 → 361、ETF 360 → 359 兩處變少，逐檔的原因都寫在 STATUS 與 commit 訊息（轉市場 3 檔、興櫃轉為公開發行 1 檔、ETF 查無代號 1 檔）。這一次只證明「正常的來源不會被誤擋」，不證明關卡擋得對——擋得對要看上面的矩陣。

**補充說明（四）第 1 點：「出現過」不等於「是理由」——逐一查了本 App 所有「從輸出判斷擋下理由」的地方**

| 地方 | 原本比對哪裡 | 結論 |
|---|---|---|
| `gatetest.sh`（推送閘門驗法） | **整份輸出**找一句話（`grep -F`） | **中了**，已改 |
| `taptest.mjs`（`tap.mjs` 自己的測試） | **整份輸出**找「母體是空的」這類字 | **中了**（實際沒被騙過，但形狀一樣），已改 |
| `mutjudge.mjs`（突變判定） | 只取「  ✗ 」那一行，細節行不算 | 原本就對；兩個方向的對照原本就有 |
| `buildtest.mjs`（F8） | 只取「不寫檔：」後面的「  · 」清單 | 原本就對；這一輪補「別處印的同一行不算」「寫檔失敗：」「字眼對邊界」的對照 |
| `gateselftest.mjs` | 只取「兩格縮排＋✓／✗＋情境編號＋句點」開頭的行 | 原本就對；對照含「縮排不對的不算」 |

- **修正前**（`gatetest.sh` 取自 `3e7b009`：先斷言新舊雜湊不同、舊版含 `has "$T/out.txt" "$must"`、沒有 `gatereason`）：造「那句話出現了，但不是它擋的」——把自查的 (a) 金鑰類改壞成不看 commit 訊息，同時在情境 7 的 commit 訊息裡多放一個 email（(b) 類會抓到那一行、把整行原樣印出來）。**舊驗法照樣判情境 7 符合、整支回 0**——也就是會替一個壞掉的自查寫下登記、放行推送。
- **修正後**：新增 `scripts/gatereason.mjs`，只在兩種位置比：「某一類的命中」要落在那一類標頭（目標命中 N ✘，N ≥ 1）底下的命中行、而且來源對得上；「某一行開頭」只認行首、而且對邊界（`scripts/gatepush.sh` 湊不到 `scripts/gatepush.sh.orig`）。同一個樣本，新驗法判情境 7 不符。`taptest` 改成只取「  ✗ 」底下的細節行。
- **兩個方向的對照**：`gatereason` 10 組（命中行出現在別類底下、來源對不上、判決字眼只在行中間、檔名只湊到前半 → 不算；位置對 → 算），`gatetest.sh` 開跑前先跑、`controltest` 每版也跑；`taptest` 1 組（字眼只在斷言名稱或 ✓ 行 → 不算；在 ✗ 底下的細節行 → 算）。
- **突變**：`RS：` 5 條（`tap` 的理由擷取看整份輸出、閘門比對命中時不看是哪一類的標頭、行首改成行中間也算、檔名不對邊界、狀況字眼不對邊界），全部紅在指定的對照上。
- 新版驗法 15 種全部符合（`gatereason.mjs` 也加進第零關的登記清單：改了它要重跑驗法）。

**補充說明（四）第 3 點：比對兩份結果前先斷言數量**——`gateselftest` 每一種變體都斷言解析到剛好 15 種、兩種順序才比；證據腳本裡算舊版數字時，也斷言擷取到的格數＝「碰巧擋下＋沒擋」的總數（這一輪就踩到一次：擷取時前面多留了空白，一格都沒對上，差點算成舊版全擋）。

**補充說明（四）第 4 點：跳脫掃描擴到 repo 所有腳本（`scripts/escscan.mjs`，進 `npm test`）**
- 母體：走訪 repo 所有 `.mjs`／`.js`／`.cjs`／`.sh`（跳過 `node_modules` 等），**拿 git 追蹤清單核對一支不漏**；跳過的要寫理由、理由過期也報。前置：全 repo 抽到的 regex 字面與字串數量要夠（實測 530 個、16422 個），證明切割沒有走偏（切割走偏時，一路把程式碼當字串，命中照樣是 0）。
- 規則：E1 regex 字面被多跳脫（`/\\s/`）、E2 字串少跳脫（`'\s'` 在 JS 裡就是 `'s'`）、E3 `.sh` 的 grep／sed／`node -e` 帶反斜線（初篩）。`.` 不在名單裡：`/\\./` 是正常寫法，分不出來——已知盲區。
- 結果：111 支、2.9 萬行，E1、E2 零命中；E3 兩處（`gatetest.sh`），是 `gatescan` 早就逐條看過的兩個例外，照樣登記理由。每一條規則與擷取本身都有兩個方向的對照。突變 `ES：` 4 條。
- **這一輪它就派上用場了**：更新一條突變的錨點時，shell 把 `\\w` 改成 `\w`，寫進 JS 字串就變成單純的 `w`——正是 E2；`checkmutations` 也同時報那條突變過期。

## 範圍外發現五件（2026-09-24，Dispatch 排的優先序）

> 從 STATUS 搬過來（原文照錄）；第 5 件後來立成 F8，見上一節。


**2026-09-24 進行中：`docs/SPEC_檢查器修補.md`**（已核可；順序 P0 → P1〔S3、S4〕→ P2〔S5、S6、S7、S9、S8〕，每件一個 commit，訊息寫編號）。
P0（S1、S2）已驗收（`84e3999`）。**S3 已做**：`assertaudit` 的判斷邏輯抽到 `scripts/auditjudge.mjs`，開頭先跑四組對照（一定失敗的斷言、空母體、寫出資料前就崩、必過的乾淨測試），沒過就回傳 1、不產報告；
同一組對照由新的 `scripts/controltest.mjs` 每版在 `npm test` 裡跑（每組用寫死的標籤點名，少一組也會紅），突變清單加了以 `S3：` 開頭的四條。**S4 已做**：`sweep` 的判斷抽到 `scripts/sweepjudge.mjs`，開頭用錄好的合成回應跑六組對照（不打網路），`controltest` 每版跑同一組，突變清單加了以 `S4：` 開頭的六條。**S5 已做**：兩份清單各配孤兒檢查（`assertaudit` 的清單對 `npm test` 鏈、`gatescan` 的 `FILES` 對整個 repo 裡像閘門的腳本），都在每版會跑的那一組。**S6 已做**：`livecheck` 的判斷抽到 `scripts/livejudge.mjs`，開頭用錄好的回應跑九組對照（不打證交所），`controltest` 每版跑。**S7 已做**：閘門第零關（回 4）比對驗法登記，`gatetest.sh` 全部符合才登記；情境改成獨立函式、可換順序。**S9 已做**：盤點表沒實測的格子都補測了（見「2026-09-24 檢查器盤點」那張表，補測的格子標「S9 補測」；刪檔類補「先確認它原本在」）。**S8 已做**（只盤點、沒改程式；結果在「2026-09-24 檢查器盤點」那一節）。**`SPEC_檢查器修補` 的 S3–S9 全部做完（Dispatch 2026-09-24 驗收：形狀都對）。**突變整套等 F1–F7 全部做完、四家一起跑。

**2026-09-24 範圍外發現的五件全部做完、已推送**（第 5 件 `3cf33ae`、第 1 件 `3e77de1`＋`1d0ba37`、第 3／4 件 `2b4aa09`、第 2 件 `c0bc4db`）。**等 Dispatch 回覆的**：`data/stocks.json` 過期（7812 已轉上市，要重跑 `build-stocks`、bump、部署——動到出貨資料，沒自己做）。突變整套照指示還沒跑（等 F1–F7 四家一起）。
原本的交辦（Dispatch 排的優先序）：第 5 件（三支 build 資料變少照樣寫檔）→ 第 1 件（`livecheck` 崩在日曆段）→ 第 3 件（`upgradecheck` 只看兩頁）→ 第 2 件（`livecheck` 沒打 `FMTQIK`）→ 第 4 件（`sweep` 不看新路由，做成孤兒檢查）。舊版一律取明確的 commit `77b026d`（這一輪動手之前的 HEAD），不用會跟著本機變的 `origin/main`；比對擋下理由時確認字串落在錯誤訊息的位置（統籌者驗 TripQuest 時，舊版輸出裡也出現同一個名字，但那是清單在列，不是錯誤訊息）。
**第 5 件已做**：後來立成 F8，見本檔上面的 F8 一節。
**第 1 件已做**（`3e77de1` 先讓崩潰判失敗、後面照跑；`1d0ba37` 再修日曆段）：`livecheck` 每一段包進 `livejudge.mjs` 的 `makeStage`，崩了就記成「livecheck 段落崩潰：<段名>」、後面照跑；日曆段改用 `calendarClosed`（多年格式攤平，兩種格式都不是就拋錯、不回空的）。對照十（日曆休市日）、對照十一（中間一段崩了、後面照跑）在 `livecheck` 開頭與 `controltest` 都跑；突變清單加了以 `LC：` 開頭的四條。
  · **三個明確 commit 對本機假證交所各跑一次**（送錄好的回應、帶 CORS）：`77b026d`（修正前）崩在第 7 段、後面兩段沒跑到、沒有總結；`3e77de1`（只加 stage）日曆段記成段落崩潰、跑到第 9 段、有總結；`1d0ba37`（修好）9 段都跑、沒有崩潰。
  · **對真的證交所實跑一次**（暫存複本，約 14 個請求）：9 段都跑完，日曆核對 2026-02、2026-04 都一致。**一項失敗是真的發現**：`data/stocks.json`（2026-09-11 產的）把 7812 標成非上市，它今天在集中市場成交——**代號表過期了**，這一段以前從來沒跑到，所以一直沒人發現。要修得重跑 `build-stocks`、更新出貨的資料檔（要 bump、部署），範圍外，**沒動，待 Dispatch 決定**。
**第 3、4 件已做**（`2b4aa09`）：新增 `scripts/routes.mjs`——`sweep` 與 `upgradecheck` 共用的逐頁清單（7 頁）、不巡的理由（`/holdings/:code`：帶參數、要有真的持股才進得去，`holdingtest` 從 `#/holdings/<代號>` 驗它），外加孤兒檢查：`js/app.js` 註冊的每一條路由都要在清單裡或寫明理由，過期的清單與理由也報。`controltest` 每版靜態比對 `js/app.js`；`sweep`、`upgradecheck` 跑的時候再比一次瀏覽器裡真的註冊的。`upgradecheck` 換版之後逐頁開 7 頁，每頁看標題、內容、沒有錯誤卡、沒有未攔截的例外。突變清單加了以 `RT：` 開頭的五條。
  · **擴大母體沒有少挑**：`sweep` 舊清單 7 條＝新清單 7 條（少掉 0、多出 0）；`upgradecheck` 舊版換版後看的頁（主畫面、持股、定期定額；擷取時另外撈到一個 `/settings`）新清單都有，新增股利、新聞、試算。
  · **第 3 件**（暫存複本、舊版取 `77b026d`）：新版股利頁一開就拋錯 → 修正前 17 項通過、回 0；修正後回 1，逐頁那條點名 `/dividends`（標題沒換到、出現錯誤卡）。**§5.12 三種擺在一起**：只拿掉錯誤卡那一道、只拿掉標題那一道都照樣紅，兩道一起拿掉才放行——錯誤畫面會把標題改成「這一頁打不開」，兩道互相補位。（寫程式時註解裡寫了「只看標題會被錯誤卡騙過」，實測不成立，已改正。）
  · **第 4 件**：多註冊一條 `/zzq`、沒登記 → `controltest` 回 1 點名 `/zzq`；本機伺服器服務那份複本，修正前的 `sweep` 20 項全過、回 0，修正後回 1 點名 `/zzq`；拿掉 `sweep` 的路由孤兒那一條又回 0。對線上實跑 `sweep` 21 項全過、`upgradecheck` 20 項全過。
  · 修正後第一次跑第 3 件時，多紅了一條「舊版的 Service Worker 已經接手這個頁面」，重跑沒再出現——時序不穩，跟這次的改動無關，記下來。
**第 2 件已做**（`c0bc4db`）：`livejudge.mjs` 登記 `livecheck` 要打的端點（`LIVE_ENDPOINTS`，刻意不打的寫進 `ENDPOINT_SKIP` 並寫理由），孤兒檢查三個方向：app 端（`js/`、`sw.js`、Worker）用到卻沒登記的、登記了 `livecheck` 卻沒打的、理由過期的；`controltest` 每版跑。`livecheck` 補一段 FMTQIK（查**上個月**，月初當月還沒資料；用 app 的 `parseFmtqik`），對照組用錄好的 `fmtqik-115-09.json`（欄位改名要報）。突變清單加了以 `EP：` 開頭的五條。
  · **修正前**（暫存複本，`livecheck` 換成 `77b026d` 的版本、先確認雜湊不同且裡面沒有 FMTQIK）：`controltest` 回 1，「端點沒打」點名 FMTQIK。修正後回 0。還沒加 FMTQIK 那一段時，孤兒檢查一接上就自己點名它。
  · **擴大擷取沒有少挑**：新的擷取樣式與 S9 盤點用的樣式，對同一批 40 支 app 檔各跑一次，都是 5 個（FMTQIK、STOCK_DAY、STOCK_DAY_ALL、TWT48U、TWT49U），少掉 0、多出 0。
  · **對真的證交所實跑**：FMTQIK 段過了（2026 年 8 月每一天都算得出指數與漲跌％）；唯一的失敗仍是第 1 件發現的代號表過期（7812）。
  · （自我修正）`1d0ba37` 加了四條突變，卻沒同步改文件裡的突變條數，`doctest` 在 `1d0ba37`、`6b7b0d1` 兩個 commit 是紅的（當時 307 條、實際 311 條）；沒改寫歷史，下一個 commit 補上。起因是 commit 接在 `doctest` 後面用的是分號不是 `&&`。

## 突變的判定改嚴之後，帶 expect 的全部重跑（`e128835`）

- **範圍**：`node scripts/mutationtest.mjs --expect-only`，帶 `expect` 的 **118 條**（全部 350 條裡的；其餘 232 條沒有 `expect`，判定改嚴不影響它們，留給突變整套）。判定是新的「只紅對應的那一種」：多紅了別組、又沒用 `alsoRed` 明列並寫理由的，一律算失敗。
- **在哪裡跑**：commit `e128835`（已推送、遠端＝本機），工作區乾淨；開跑前檢查沒有殘留的突變，跑完檢查所有被改過的檔案都已還原。
- **時間**：2026-09-24 20:18 開跑、20:58 結束，約 40 分鐘。
- **結果**：基準全綠；**118 條全部紅在指定的那一條，0 條沒紅、0 條紅錯地方、0 條多紅了別組**；突變清單本身三項（沒有重複、都寫了理由、全部還原）也過。總計 138 項通過、0 項失敗，回 0。
- **沒紅的清單**：無。
- 完整的 log 在 `.logs/mut-expect-2026-09-24-e128835.log`（不進版控）。

**這一條結果之前作廢過兩次（註記）**
1. 第一次：`gateselftest` 要跑約 7 分鐘，`mutationtest` 每支測試一律 180 秒逾時，基準就被判成不綠。改成逐支設定逾時（`3e7b009`）。
2. 第二次：跑到一半，我把新寫的 `scripts/gatereason.mjs` 放進 repo，基準那一步的孤兒檢查當場點名這支「沒登記的新檔」，整批作廢。**那不是孤兒檢查誤報，是它正確地報了**：那一刻 repo 裡確實多了一支沒登記的腳本，跑出來的結果也確實不是對著任何一個 commit。之後的做法：改動全部做完、commit 之後才跑；跑的期間不碰 repo；在暫存複本跑之前，先斷言複本裡確實有這次新加的段落（統籌者踩過：clone 到的是已 commit 的版本，新段落還沒 commit，跑到的是舊的掃描）。

## F9：build 驗法登記（`3438e7d`、`59fcbd6`；2026-09-24 統籌者新訂，四家統一）

**做法**（`scripts/buildverify.mjs`）
- **登記**（`node scripts/buildverify.mjs`）：一開跑就刪舊登記。被守的 9 支檔（三支 build、`buildguard`、`testfetch`、`buildtest`、`tap.mjs`、`js/roc.js`、`js/twse.js`）要全部存在；build 與驗法 import 到的每一支本地檔都要在清單上（從入口追 import，加上 `buildtest` 另開程序跑的 `SCRIPTS` 表）。工作區要跟 HEAD 一模一樣，否則不登記。`buildtest` 要回 0，而且行首的矩陣總計要是「全擋」。跑的期間檔案不能變。全部過了，才寫 `.logs/build-verified.txt`：HEAD 的 commit 與 9 支檔 HEAD 版本的雜湊。
- **推送前比對**（`gatepush.sh` 在 fetch 之後、自查之前呼叫 `--check`，沒過回 5）：逐個 commit 列出這次要推的 commit 動到的檔。commit 數要跟 `rev-list --count` 對得上，對不上就當檢查器壞了、停下。**沒動到被守的檔就不看登記**；動到了，登記要跟**要推的已 commit 版本**（`<ref>:<檔>`）一致，不是跟工作區比。
- 閘門那五支照舊由第零關守，每次推送都比對；`buildverify.mjs` 也加進第零關的登記清單（現在六支），改了它要重跑閘門驗法。
- 第一次真的登記（`3438e7d`）：`buildtest` 78 項通過、矩陣 54 格全擋、9 支檔。

**六種必備情境（統籌者原文，2026-09-24 由 Dispatch 逐字轉來）逐條對照**

閘門驗法（`scripts/gatetest.sh`）的情境 13–18 各守一條。每一種靠**不同的機制**擋下或放行，所以各有一條突變只紅它：
- 每一種情境一開始都先放一份「對起點」的 build 登記。沒動到被守的檔的情境都帶著登記，只有 17 專門把它拿掉。
- 15、16、14 在複本裡**真的跑** `node scripts/buildverify.mjs`（14 跑完整的 `buildtest`）。

突變都在 `gateselftest`，改的是複本裡的閘門或 `buildverify.mjs`，commit 之後跑整套閘門驗法，看「不符的情境」是不是剛好那一種。正反兩種順序都跑。

**對照表（補充說明（八）第 2 點的格式：必備情境／靠哪個擋法／只紅它的突變）**

規則：每一種必備情境要由**不同的擋法**擋下（或放行）。所有情境共同依靠的那一環另列一列，也配一條突變。情境內容見下面「情境細節」那張表。

| 必備情境（統籌者原文） | 靠哪個擋法 | 只紅它的突變（gateselftest） |
|---|---|---|
| 1. 改了被守的檔、沒跑驗法就推 → 擋、點名驗法（情境 13） | **比雜湊**：登記的雜湊 ≠ 要推的已 commit 版本 | G7「有登記就放行、不比雜湊」→ 剛好 13 |
| 2. 跑過驗法之後同一個改動 → 推得出去（情境 14） | **比的是要推的版本**（`<ref>:<檔>`），不是工作區 | G5「比工作區」→ 剛好 14 |
| 3. 驗法沒全過 → 登記被刪 → 推不出去（情境 15） | **驗法一開跑就刪舊登記** | G8「一開跑不刪舊登記」→ 剛好 15 |
| 4. 工作區有改動時跑驗法 → 不登記（情境 16） | **登記前比對工作區＝HEAD** | G9「算不出工作區有改動」→ 剛好 16 |
| 5. 沒動到、也沒有登記 → 推得出去（情境 17，專用） | **沒動到被守的檔就不看登記** | G10「沒動到也要有登記檔」→ 剛好 17 |
| 6. 前一個 commit 動到、最後一個乾淨 → 擋（情境 18） | **逐個 commit 列出動到的檔**（範圍是這次要推的每一個 commit） | G6「只看最後一個 commit」→ 剛好 18 |
| （共同依靠）閘門照 F9 的結果停下 | `gatepush.sh`：`buildverify --check` 回非 0 就回 5 | G4「F9 比對沒過也照樣往下推」→ 13、15、16、18 |
| （共同依靠）沒有登記檔就擋 | `buildverify --check`：動到了、登記檔不在就停 | G11「沒有登記檔也往下比」→ 15、16、18 |

- **偵測共用還是處置共用**（補充說明（十一）第 2 點的尺，措辭來自 MealMate）：
  - 第 3、4、6 條的**偵測各自獨立**：驗法一開跑就刪舊登記（G8 只紅 15）、登記前比對工作區＝HEAD（G9 只紅 16）、逐個 commit 列出動到的檔（G6 只紅 18）。
  - **只有處置共用**：推送那一刻三條都落在「沒有 build 驗法登記」就擋。防線沒有變少，不改設計；處置那一環另列一列、配 G11（三條一起紅）。
  - 「閘門照 F9 的結果停下」也是處置，會擋的四種都靠它，另列一列、配 G4。
- G11 也是一個**隱式的擋**：突變把「沒有登記檔就停」拿掉之後，後面讀登記檔會丟例外、整支崩掉。閘門照樣回 5，但沒有講出理由，所以三種情境都判不符（見下一節）。
- 第 1 條以前也靠「沒有登記」擋，跟第 3、4 條共用一道防線。這是「只紅那一種」的要求反過來揭露的：現在每種情境一開始都放一份「對起點」的登記，第 1 條改成靠「登記對不上」擋。

**情境細節**

| 原文 | 由哪一種情境守著 | 只紅它的突變 |
|---|---|---|
| 1. 改一支被守的檔、commit、沒跑那支驗法就推 → 擋在第零關、點名驗法 | **13**：登記是改之前那一版（跑過、後來又改）→ 回 5，理由那一行要從行首起一字不差，包括「——先跑 node scripts/buildverify.mjs」 | G7「有登記就放行、不比雜湊」→ 剛好 13 |
| 2. 跑過那支驗法（全過）之後同一個改動 → 推得出去 | **14**：真的跑 `buildverify`（登記的 commit＝HEAD），之後工作區再改一行、不 commit → 回 0（比的是要推的版本，不是工作區） | G5「比工作區、不比要推的已 commit 版本」→ 剛好 14 |
| 3. 那支驗法沒全過 → 登記被刪（先放一份舊登記再跑、跑完它不在）→ 動到被守的檔就推不出去 | **15**：先確認舊登記在；commit 一個讓 `buildtest` 語法錯誤的改動；真的跑 `buildverify` → 要回非 0，而且停的理由是「驗法沒過」；跑完舊登記不在（不在就記一筆不符）；推送回 5，理由是「沒有 build 驗法登記」 | G8「驗法一開跑不刪舊登記」→ 剛好 15（舊登記留著，推送改成被「對不上」擋下：照樣回 5，但理由不對，也算紅） |
| 4. 被守的檔工作區有改動時跑驗法 → 不登記（不能寫入新的） | **16**：先拿掉起點的登記（讓「刪或不動」都不影響結論）；commit 一個改動，工作區再改一行；真的跑 `buildverify` → 不能有登記檔，而且理由要是「工作區跟 HEAD 不一樣：scripts/buildguard.mjs」；推送回 5 | G9「算不出工作區有改動」（一律當成跟 HEAD 一樣）→ 剛好 16：驗法會真的跑完 `buildtest`、寫入新登記、推送放行 |
| 5. 沒動到被守的檔、也沒有登記 → 照常推得出去（專用情境） | **17**：先確認起點的登記在、再拿掉；疊一個沒動到的 commit → 回 0，理由是「沒動到 build 與它的驗法，不看登記」 | G10「沒動到被守的檔也要有登記檔」→ 剛好 17（其他沒動到的情境都帶著登記，所以不受影響） |
| 6. 前一個 commit 動到被守的檔、最後一個 commit 乾淨 → 仍要擋 | **18**：沒有登記；動到的在前一個 commit，最後一個只動 `gatetest-note.md`（前提斷言）→ 回 5 | G6「只看最後一個 commit 有沒有動到」→ 剛好 18 |

- **閘門整個不理 F9**（G4：`gatepush.sh` 不看 `buildverify` 的回傳值）→ 13、15、16、18 不符。這一條不屬於任何一種，四種會擋的都靠它。
- 舊的 12 系列（12、12b、12c、12d、12e）已經併進上面六種：12e「工作區另有改動」併進 14，12d 變成 17（專用情境，不再靠「其他情境剛好沒有登記」）。
- `buildverifytest`（每版跑）照樣守著判斷那一層：「不登記」的每一種分支，以及比對時的每一種情況，各有情境與 `F9：` 突變。

另外，「不登記」的分支每一種都有情境，也都有一條突變只紅它：驗法沒過、沒有全擋、沒有總計、總計不在行首、跑的期間改了檔、相依不在清單上、清單上的檔不見了、範圍算不出（回非 0，不當成沒動到）。

**修正前**：F9 是新加的關卡，沒有「舊版的 F9」可以比。替代的證明是 G4：它等於沒有 F9 的閘門，會擋的四種全部推得出去。

**失敗路徑（補充說明（五）F10）**：「取不到檔名清單就停」這個分支，在 `buildverifytest` 用一個真的不存在的範圍觸發（不是後門）。突變「範圍算不出當成沒動到」只紅這一條。
- **已知限制（統籌者實測認可，F10 第 1b 點第 3 種）**：在閘門那一層，F10 的做法是在 `PATH` 前面放一個假的 `git`，只讓 `git log --name-only` 失敗。但 `gatepush.sh` 是經由 `node` 呼叫 `git` 的。統籌者在 Windows 上逐一試過四種造法：
  - 叫 `git` 的 shell 腳本：node 不理它，跑到真的 git。
  - `git.cmd`：node 不理它，跑到真的 git。
  - 把 node.exe 改名成 `git.exe`：node 會把 git 的參數當成自己的吃掉。
  - `GIT_DIR` 指向不存在的目錄：真的 git 會失敗，但不能只讓某一個子指令失敗。這裡如果用它，閘門自己的 fetch 會先失敗、在第一關就停，走不到 F9 那一段。

  所以這一層造不出只有 F9 失敗的情況。
- **替代的證明**：
  - 判斷在 `buildverify.mjs --check` 裡，由上面那條情境與突變守著；
  - 閘門只剩「回傳值不是 0 就回 5」那一行，由 G4 守著。

**突變與實跑結果**
- `--only 'F9：'`（`3438e7d`）：11 條全部只紅指定的那一條。其中「一開跑不刪舊登記」本來就會連帶紅另外七種「不登記」情境：每一種都先放了舊登記，用 `alsoRed` 列出並寫了理由。
- 閘門驗法（`59fcbd6`）：20 種全部符合。
- **真的推送走過一次「沒動到就不看登記」**：推 `56dfec2` 時（4 個 commit，都沒動到被守的檔），閘門印出「F9：這次要推的 4 個 commit 沒動到 build 與它的驗法，不看登記」，然後放行。這條規則不只在測試裡、也在真的推送上照規則走了一次。
- `gateselftest`（`7e998fc`，補充說明（八）之後）：87 項全過，約 48 分鐘。22 種情境，正反兩種順序各跑一次、逐一相同。G1–G10 的結果跟下一行一樣；**G11 → 剛好 15、16、18，G12 → 剛好 4b**。
- `gateselftest`（照六條原文重排之後）：`357f408`，75 項全過，約 31 分鐘。原樣與倒序各一次：21 種全部符合、逐一相同（兩邊都斷言剛好 21 種）。G1 → 剛好 10、11；G2 → 剛好 10；G3 → 剛好 11；G4 → 剛好 13、15、16、18；**G5 → 剛好 14；G6 → 剛好 18；G7 → 剛好 13；G8 → 剛好 15；G9 → 剛好 16；G10 → 剛好 17**。每一種突變都確認驗法沒全過時，先放的舊登記被刪掉。閘門驗法本身（`357f408`）：21 種全部符合
- 較早一版 `gateselftest`（`59fcbd6`，當時是 12 系列）：51 項全過，約 17 分鐘。原樣與倒序各一次：20 種都符合、逐一相同（兩邊都斷言剛好 20 種）。G1 → 剛好 10、11；G2 → 剛好 10；G3 → 剛好 11；**G4 → 剛好 12、12b；G5 → 剛好 12e；G6 → 剛好 12b**。每一種突變都確認驗法沒全過時，先放的舊登記被刪掉。

## F10：失敗路徑的常設驗法（2026-09-24，補充說明（五）；三支入口＋自查的訊息與作者欄）

**三支入口的「對照組沒過就停」**（`scripts/entrygatetest.mjs`，進 `npm test`）
- **修正前**：`assertaudit`、`sweep`、`livecheck` 開頭都先跑對照組，沒過就 `process.exit(1)`。每版被驗的只有判斷邏輯（`controltest`）；入口「照判斷停下」那一行只實跑過一次（S3、S4、S6 的證據），拿掉它沒有任何測試會紅。
- **做法**（F10 第 1b 點第 1 種：讓真的依賴失敗，正式程式裡沒有為測試開的分支）：
  - 在 `.logs/` 放一份 scripts／js／data 的複本，把入口依賴的東西真的弄壞，再從入口跑：`assertaudit` 弄壞 `auditjudge`、`sweep` 弄壞 `sweepjudge`、`livecheck` 弄壞一份錄好的回應。
  - 網路用預先載入的模組把 `fetch` 換掉：記下網址、然後結束，一律不打真的網路。
- **每一支兩個方向**：
  - 依賴壞了 → 回 1、「對照組沒過」那一行在行首、沒有往下做（沒打網路，或沒開始跑測試）。
  - 依賴沒壞（對照）→ 過了對照組、真的往下做。這一條證明「沒往下做」不是因為本來就走不到。
- **第一版自己踩到的**：`livecheck` 那一格一開始沒擋——我弄壞的是 `holiday-schedule-115.json`，對照組根本不讀它。改成對照組真的會讀的 `twt48u-forecast.json`。另外，細節裡原樣帶了子程序的輸出，裡面「  ✗ …」開頭的行被突變判定當成這支測試自己的失敗；改成壓成一行，並加對照。
- **突變**：`F10：` 3 條（三支各把 `if (ctrl.some(…))` 改成 `if (false)`），各只紅自己那一支的「依賴壞了」。
- 三支入口剩下的那一行都有常設情境守著，**沒有需要列為已知限制的**。

**自查：有 commit 卻取不到訊息與作者欄**（Dispatch 轉 MealMate 與統籌者各中一次的位置）
- **修正前**（`precheck.mjs`，`0f5b767`）：取訊息與作者欄用一次 `git log --format=…`。取到空的就當成 0 行、0 命中、通過；沒有像新增行那樣拿另一個來源核對。git 本身失敗則是例外往外丟、整支崩掉（隱式的擋）。
- **修正後**：抽成 `commitMeta(git, rev)`，「執行 git」的函式當參數傳進去（F10 第 1b 點第 2 種）。
  - 拿 `git rev-list --count` 數出的 commit 數核對：作者欄、提交者欄各要剛好那麼多行。
  - 全部取不到、只取到一部分、git 失敗、commit 數算不出來，都擋並講明原因。
  - 隱式的擋改成明寫：catch 之後講明，不丟給外層。
- **常設情境**（`controltest`，每版）：
  - 用假的 git 造出以上每一種失敗；另外一條檢查單一 commit 有沒有限定 `-1`。
  - 兩個方向的必過：0 個 commit 的範圍放行、正常的兩個 commit 放行。
  - 真的 git：`GIT_DIR` 指向不存在的目錄，真的 git 失敗 → 擋（第 1 種）；真的 HEAD → 放行。
- **突變**：`PC：` 4 條，都紅在對應那一條。
  - 「git 失敗被吞掉」就是 Dispatch 說的隱式擋的突變寫法：把例外吞掉、當成空的。
  - 「commit 數算不出來也往下比」：後面的核對照樣會擋，但理由不對，一樣算紅。
  - 有兩條會連帶紅，用 `alsoRed` 列出：「全部取不到／只取到一部分」靠同一道核對；三種 git 失敗落在同一個 catch。
- **§5.14 真實資料**：最近 60 個 commit 逐個跑，0 個被新核對擋下（訊息與作者欄共 694 行）。
- **已知限制**：`main()` 裡「problem 不是 null 就 exit(1)」那一行，沒辦法只讓訊息那一個子指令失敗（`GIT_DIR` 會讓新增行那一段先失敗）。替代的守法：判斷全在 `commitMeta`，由上面的情境與突變守著；那一行本身跟 `if (meta.problem)` 一樣短。

## 突變補 `expect`：第一批（2026-09-24，Dispatch 裁示分批補）

- **先做的對照組**：`checkmutations` 新增「expect 寫錯成另一組存在、但沒紅的標籤 → 判成紅錯地方」。判定器的兩條突變會連帶紅它，已補 `alsoRed`，重跑都合格。
- **這一批**：只用 node、跑得快的 7 支測試（`shelltest`、`calctest`、`parsetest`、`roctest`、`doctest`、`fmttest`、`changestest`）底下，54 條沒寫 `expect` 的突變。
  - 做法：用 `scripts/evidence/collate.mjs` 逐條套上、取出實際紅了哪幾條。
  - 只紅一條的，`expect` 就是它。
  - 紅好幾條的，挑最直接的那一條當 `expect`，其餘列 `alsoRed`，並逐條寫連帶的理由。
- **結果**：7 支測試底下的突變全部「只紅對應的那一種」——`shelltest` 25、`calctest` 10、`parsetest` 8、`roctest` 6、`doctest` 13、`fmttest` 2、`changestest` 2，0 條不合格。
- **補的過程中查出的三件**：
  1. **一條突變本身是壞的**：「沒填完的那一檔當成 0 照樣算進去」寫的是 `leg = {…}`，但 `leg` 是 `for…of` 的 const，一跑就 TypeError。測試是被語法錯誤弄崩的，**這條突變從來沒驗到它 why 講的行為**。已改成 `Object.assign(leg, …)`，現在紅在「沒填完的那一檔沒有算」。
  2. **一條靠整支崩掉才紅**：「view 炸了只印 console」。`shelltest` 等錯誤卡用 `waitForSelector`，等不到就逾時崩掉，沒有任何斷言紅。已改成明寫的斷言「view 丟例外時，畫面上出現錯誤卡」。
  3. **`calctest` 有三條斷言訊息用數字開頭**：沒有穩定的錨點，依 §5.9 加了固定標籤（「純複利十年對得上手算：」「配息再投入十年對得上手算：」「股數法與金額法的差距小於一股：」）。
- **整理工具自己的毛病**：`collate.mjs` 在突變還沒寫 `expect` 時用 `includes(undefined)`，會把訊息裡剛好印出 undefined 的失敗濾掉，漏列成 `alsoRed`；新判定跑出兩條「多紅了別組」才發現。已修，以後的批次不會再漏。
- **還剩 178 條**（全部 372 條，帶 `expect` 的 194 條，以 `scripts/mutationtest.mjs` 的 `expect:` 數得出來）。

## 補充說明（八）（2026-09-25）

**第 1 點：「取到了卻是空的 commit 訊息與作者欄」有常設情境——確認過了。** 由 `controltest` 每版跑，都從 `precheck.mjs` 的 `commitMeta` 進去，「執行 git」的函式當參數傳入：

| 情況 | 預期 | 情境標籤 |
|---|---|---|
| 有 2 個 commit，取到空的 | 擋 | 自查訊息與作者：有 commit 卻取到空的 |
| 有 2 個 commit，只取到 1 組 | 擋 | 自查訊息與作者：只取到一部分 |
| 取訊息的 git 失敗、數 commit 的 git 失敗 | 擋並講明 | 自查訊息與作者：取訊息的 git 失敗／數 commit 的 git 失敗 |
| commit 數算不出來 | 擋，理由是「算不出」 | 自查訊息與作者：commit 數算不出來 |
| 真的 git 失敗（`GIT_DIR` 指向不存在的目錄） | 擋 | 自查訊息與作者：真的 git 失敗 |
| 0 個 commit／正常 2 個／真的 HEAD | 放行（必過） | 自查訊息與作者（必過）：… |

突變 `PC：` 4 條，每一條都紅在對應的那一條（見 F10 一節）。閘門那一層：每次推送都跑這一段，而且推送時會印出「commit 訊息與作者 N 行」；推 `fe11a74` 時是 2 個 commit、21 行。

**第 3 點（F10 第 5 點）：隱式的擋，突變怎麼做**——本 App 找到的每一格：

| 分支 | 為什麼是隱式的 | 突變怎麼做（讓那段邏輯放行） | 守它的情境 |
|---|---|---|---|
| 閘門第三關：`ls-remote` 讀不到遠端 | 沒有「讀不到就停」這一句，靠「讀到空的 ≠ 本機」擋下 | G12：讀不到當成一樣（`[ -n "$REMOTE_SHA" ] && …`）→ 只紅 4b | 閘門驗法 4b（2026-09-25 新增）。造法：讓假遠端的 upload-pack 在推送完成之後失敗，推送前的 fetch 照常 |
| F9：動到了、登記檔不在 | 拿掉「不在就停」之後，後面讀登記檔丟例外、整支崩掉，閘門照樣回 5 | G11：把那一句拿掉，讓它往下讀（崩掉）→ 15、16、18 回 5，但沒講出理由，判不符 | 閘門驗法 15、16、18 |
| 自查：git 失敗 | **以前**是例外往外丟、整支崩掉。現在改成明寫的 catch、講明原因 | PC：把例外吞掉、當成 0 個 commit | controltest「取訊息的 git 失敗」等三條 |
| 自查：commit 數算不出來（NaN） | 就算沒有這一句，後面「作者欄數 ≠ commit 數」的核對也會接住，照樣擋 | PC：拿掉這一句 → 照樣擋，但理由變成「取到 N 個作者欄」，判不符 | controltest「commit 數算不出來」 |
| F9 第 3 條：驗法沒全過卻留著舊登記 | 舊登記對不上要推的版本，後面的比雜湊接住，照樣回 5 | G8：不刪舊登記 → 回 5，但理由是「對不上」，不是「沒有登記」，判不符 | 閘門驗法 15 |

F10 三支入口（`assertaudit`、`sweep`、`livecheck`）的停都是明寫的 `if (…) process.exit(1)`，不屬於這一型。

**「正式閘門不讀測試用環境變數」——本 App 其實中了，已修（2026-09-25）**
- `gatepush.sh` 以前有 `PRECHECK="${PRECHECK:-$HERE/precheck.mjs}"` 與同樣寫法的 `PIISCAN`。本意是讓驗法 2a、2b 餵「壞掉的檢查器」，但同一個開關也能用 `PRECHECK=<一支直接回 0 的檔>` 把第一關整個換掉。跟 TripQuest 那一型相同。
- 修法：閘門寫死用自己旁邊那兩支。驗法 2a、2b 改成在複本的工作區直接把檢查器改壞，再照改壞的內容重新登記第零關。改完 22 種全部符合，2a、2b 照樣被對照組擋下。
- 壞寫法掃描（`gatescan`）新增 `env-switch`：抓 `X="${X:-…}"` 與 USERNAME／USER 以外的 `process.env`。
  - 白名單只有這兩個：自查 (c) 類要知道本機使用者名稱。
  - 對照組：正例 2 個（真的出過事的原文，加上一個 TripQuest 型的合成樣本），反例 3 個。
  - 突變 `GS：` 1 條，紅在 `env-switch` 那一組。
  - 掃出一處誤報：`gatetest.sh` 裡 `PRE_BAD="${PRE_BAD:-…}"` 是區域變數的預設值，改寫成 `if` 判斷。
  - 登記一條例外：`buildverifytest` 寫出來的假 buildtest 讀 `STUB_MODE`，那是測試用的假檔，不是閘門。

## 補充說明（十一）（2026-09-25）

**第 1 點：作者信箱、提交者信箱各自一種情境、一條突變——本 App 中了一半，已補**
- **修正前**（`500e6cc` 的閘門驗法）：
  - 情境 7b 用 `git -c user.email=…` 造樣本，**作者與提交者兩個信箱同時**是一般信箱。
  - 7b 的理由比對要求命中落在「作者：」那一行，所以「只拿掉作者信箱」會紅。但**沒有任何情境只靠提交者信箱**。
  - **實測**：在 `500e6cc` 的複本（`main` 指到那一版）把自查改成不取提交者信箱、commit，跑閘門驗法 → **22 種全部符合、回 0**。也就是會替一個壞掉的自查寫下登記、放行推送。
  - 第一次量的時候複本是 detached HEAD，驗法停在「假遠端初始化失敗」、回 1。那不是量到結果，已丟掉重量。
- **修正後**：
  - 7b 只有作者是一般信箱（`GIT_AUTHOR_EMAIL`），7c 只有提交者是一般信箱（`GIT_COMMITTER_EMAIL`）；兩種都先斷言另一欄確實是 noreply。
  - 理由比對分別要求命中落在「+作者：」「+提交者：」那一行。
- **突變**（`gateselftest`）：G13 自查不取作者信箱 → 剛好 7b；G14 自查不取提交者信箱 → 剛好 7c。
- 之前報的「作者行數、提交者行數都要等於 commit 數」是**筆數檢查**（有沒有取到），不是內容檢查（取到的信箱有沒有命中）。兩道各自有情境：筆數在 `controltest`，內容在閘門驗法 7b、7c。

**第 2 點：偵測共用還是處置共用**——照這把尺重看第 3、4、6 條，結果是**偵測各自獨立、只有處置共用**，不改設計（見 F9 一節的對照表與說明）。

**第 3 點：對照樣本要打到檢查規則的每一個分支——已做（2026-09-25）**

| 檢查 | 以前 | 現在 | 突變（每一條只紅它那個分支的樣本） |
|---|---|---|---|
| 自查 (a) 金鑰 | 4 個分支，樣本只有 `gh` 一種 | 4 種各一個樣本 | `PB：` 4 條 |
| 自查 (b) email | 排除 noreply 的 3 個分支沒有反例 | noreply@github.com、noreply@anthropic.com、`@users.noreply.github.com` 各一個反例 | `PB：` 3 條 |
| 自查 (d) 路徑 | 磁碟機斜線、`/Users/` 沒有自己的樣本 | 反斜線、斜線、`/home/`、`/Users/` 各一個 | `PB：` 4 條 |
| 第五類 語境樣式 | 9 個分支，一個樣本打到 3 個 | 指認詞 5、代號 2、金額 1，各一個樣本；另有兩個反例 | `PP：` 8 條 |
| `gatescan` 各規則 | 「接管線」9 個關鍵指令只打到 4 個；`format-no-meta` 缺 `--patch`；`absent-assert` 一個樣本打兩個分支；`plus3-header` 缺「'+++' 字面」 | 每個分支一個樣本 | `GB：` 25 條 |
| `buildtest` 判斷函式 | 目錄雜湊沒有子目錄的樣本；堆疊判斷只有「帶括號」的樣本 | 各補一個（不帶括號正是模組頂層崩掉的樣子） | `BB：` 2 條 |

- **自查與第五類的做法**：
  - 搜尋式與樣本抽到模組層（`controlResults`、`contextControls`），正式的推送與 `controltest` 用同一份。
  - 對照組從「命中 > 0」改成「每個樣本都要判對」，顯示成 N/M。
  - `controltest` 每個樣本一條斷言，登記的樣本清單要一字不差（母體用登記制）。
- **樣本之間不能共用零件**：第五類第一版的代號樣本都用「使用者的」當指認詞，拿掉「使用者的」這個分支，代號樣本也一起紅，判定報「多紅了別組」。改成每個樣本只有被測的那個分支是唯一依靠，其他部分都備兩份。
- **等價的分支，照實寫明、不做突變**（這三支沒有辦法造出只打到它的樣本）：
  - 自查 (b) 開頭的 `(users\.)?`：拿掉它，`noreply@github.com` 這段子字串照樣對得到。
  - 第五類金額的「有千分位」那一支：被「任何數字＋元」那一支包含。
  - `plus3-header` 的 `startsWith('+++')` 那一支：它的樣本一定含 `'+++'` 字面，被字面那一支包含。
- 閘門驗法 2a 跟著改成把金鑰那一支搜尋式弄壞，現在應該報「對照組命中 3/4 ✘（搜尋式壞了：gh 權杖）」，實測照樣擋下。

**補 `expect` 第二批（2026-09-25）**
- **範圍**：另外 6 支只用 node 的測試（`plantest`、`dividendtest`、`settletest`、`datatest`、`concentrationtest`、`throttletest`），共 36 條沒寫 `expect`。其中 **11 條的突變檔在凍結區**（`js/avgcost.js`、`js/settle.js`、`js/money.js`），照凍結規則不碰、不跑；這一批補 25 條。
- **驗證方式**：沒有用 `--only <測試>`，因為那樣會把凍結區那 11 條也一起跑。改成只跑這 25 條，套上突變後用 `mutjudge.judge` 逐條判，跟 `mutationtest` 是同一個判定器。結果 25 條全部「只紅對應的那一種」。
- `dividendtest` 兩條以數字開頭的斷言訊息，照 §5.9 加了固定標籤（「參考價逐筆對得上：」「有配息待公告的筆：」）。
- **還剩 153 條**（全部 419、帶 `expect` 的 266）：
  - 142 條在瀏覽器類測試，照 Dispatch 指示沒有為了補完硬跑；
  - 11 條的突變檔在凍結區，照凍結規則不碰。

**結果**：閘門驗法（`89cc0eb`）23 種全部符合。`gateselftest`（`89cc0eb`）99 項全過，約 54 分鐘，正反兩種順序 23 種逐一相同；G1–G12 跟上一版一樣，G13 → 剛好 7b，G14 → 剛好 7c。

## 收尾（2026-09-25）

**最後一次 `gateselftest`**（跑在併 commit 之前的 `640ec41`；它跟推上去的 `188b4fa` 只差證據檔、STATUS、`dividendtest` 與 `mutationtest`，閘門、驗法、自查、第五類、`buildverify`、`buildtest`、`gateselftest` 八支逐一比過雜湊都相同；對照樣本打到每一個分支、驗法 2a 改寫之後）：99 項全過，約 40 分鐘。
- 23 種情境，正反兩種順序各跑一次，逐一相同。
- 16 個變體（原樣、倒序、G1–G14）的「不符的情境」都剛好是預期的那幾種。
- 閘門驗法 23 種全部符合；build 驗法登記 54 格全擋（同上，跑在 `640ec41`）。

**Dispatch 收尾時另外點名的兩件，只確認、沒有改**
1. **每一個能單獨被執行的入口，擋不擋 git 自己認得的環境變數**：`gatepush.sh`、`precheck.mjs`、`piiscan.mjs`、`buildverify.mjs` 四支都能單獨執行，**四支都沒有擋**。
   - **實測**：在另一個 repo 放一個訊息帶合成 token 的 commit，設 `GIT_DIR` 指向它再跑 `node scripts/precheck.mjs HEAD`。自查讀的是那個 repo（回 1，輸出裡有那個 commit 的訊息）。沒設時照常讀本 repo（回 0）。
   - **推論的影響**：
     - 從閘門進來時，fetch、自查、推送都用同一個 `git -C` 加同一組環境，`GIT_DIR` 會讓三者一起換成另一個 repo。掃的就是推的，不會「掃 A、推 B」。
     - 改 git 輸出格式的那一類設定（`GIT_CONFIG_*`）會先撞上兩道核對：新增行數要等於 `--numstat`、作者欄與提交者欄的筆數要等於 commit 數。
     - 這兩點都是讀程式推的，沒有逐一實測。
   - **沒做**：照 Dispatch 指示不開新項目。要做的話照統籌者的建議：用前綴寫法拒絕 `GIT_*`（白名單例外），並配一組對照組——該攔的攔、不該攔的不攔。
2. **例外的登記顆粒度**（MealMate 提的形狀）：`gatescan` 9 條、`escscan` 7 條例外，比對方式是「同一支檔、同一條規則、那一行含這段字」。
   - **實測**：16 條每一條現在都剛好對到 1 行。
   - 但沒有任何檢查要求「剛好一行」。其中兩條的字很通用（`gatetest.sh` 的 `node -e "`、`b5old.sh` 的 `grep -E "^`），同一支檔以後再加一行同樣開頭、真的有問題的寫法，會被同一條例外一起放行。
   - 閘門第零關與 F9 的登記是整支檔的雜湊，改任何一個字都要重跑驗法，這一層沒有這個問題。
   - **沒做**。要做的話：每條例外要剛好對到一個命中，多了就報；配一個合成樣本（同一支檔多一行同樣開頭的壞寫法）證明會紅。

## 盤點：哪些有常設情境守著、哪些只是當時跑過一次（2026-09-24 收尾；補充說明（五）第 3 點，照 JLPT `552bf82` 的分法）

**有常設情境守著的**（改壞了會有東西紅；突變條數以 `scripts/mutationtest.mjs` 的名稱前綴數得出來）

| 項目 | 現在由誰守著 | 突變 |
|---|---|---|
| S1 `assertaudit` 寫出資料前就崩 → 判沒收到資料 | `controltest`（每版）的 assertaudit 對照三 | `S3：崩掉的測試不再判成「沒收到任何資料」` |
| S2 `tap.mjs` 母體非空 | `taptest`（每版） | `tap：` 3 條 |
| S3 `assertaudit` 必敗對照組 | `controltest` 的 assertaudit 對照一～四 | `S3：` 4 條 |
| S4 `sweep` 合成對照組 | `controltest` 的 sweep 對照一～六 | `S4：` 6 條 |
| S5 兩份清單的孤兒檢查 | `controltest`、`gatescan`（每版） | `S5：` 9 條 |
| S6 `livecheck` 錄音對照組 | `controltest` 的 livecheck 對照一～九 | `S6：` 9 條 |
| S7 閘門第零關、驗法可換順序 | 閘門驗法 20 種（改過閘門不重跑就推不出去）；`gateselftest` G1–G3（改過閘門或驗法時跑） | `GV：` 2 條（指向 `gateselftest`） |
| 突變判定「只紅對應的那一種」 | `checkmutations`（每版） | `判定：` 4 條 |
| F8 三支 build 的關卡與寫檔 | `buildtest`（每版，54 格矩陣＋寫檔失敗）；F9 讓「改了 build 沒重跑」推不出去 | `BG：` 25 條、`WF：` 4 條、`RS：狀況字眼不對邊界` |
| F9 build 驗法登記（六種必備情境） | `buildverifytest`（每版）；閘門驗法 13–18（改過閘門不重跑驗法就推不出去）；`gateselftest` G4–G10（改過閘門或驗法時跑） | `F9：` 11 條；gateselftest 裡每一種一條只紅它 |
| 一次性量測腳本的登記（`scripts/evidence/`） | `doctest`（每版）：沒登記、登記了卻不在、檔頭跟登記不一樣、證據檔沒有那一段，都報 | `EV：` 4 條 |
| 補充說明（四）第 1 點：理由只在錯誤訊息的位置比 | `gatereason --selftest`（閘門驗法開跑前、`controltest` 每版）；`taptest` | `RS：` 5 條 |
| 第 3 點：比對前先斷言兩邊數量 | `gateselftest` 每一種變體都斷言剛好是 ALL 那幾種 | — |
| 第 4 點：跳脫掃描＋孤兒 | `escscan`（每版） | `ES：` 4 條 |
| 範圍外第 1 件：`livecheck` 段落崩了照跑 | `controltest` 的 livecheck 對照十、十一 | `LC：` 4 條 |
| 範圍外第 2 件：端點登記 | `controltest` | `EP：` 5 條 |
| F10 三支入口的「對照組沒過就停」 | `entrygatetest`（每版）：在複本裡讓真的依賴失敗，從入口跑 | `F10：` 3 條 |
| 閘門第三關：推送後讀不到遠端（隱式的擋） | 閘門驗法 4b；`gateselftest` G12（改過閘門時跑） | G12 只紅 4b |
| 每個分支各有合成樣本（自查、第五類、gatescan、buildtest） | `controltest`、`gatescan`、`buildtest`（每版） | `PB：` 11、`PP：` 8、`GB：` 25、`BB：` 2 |
| 自查的作者信箱、提交者信箱（各自） | 閘門驗法 7b、7c；`gateselftest` G13、G14 | G13 只紅 7b、G14 只紅 7c |
| 閘門不讀環境變數換檢查器 | `gatescan` 的 `env-switch`（每版） | `GS：` 1 條 |
| 自查：有 commit 卻取不到訊息與作者欄 | `controltest`（每版）：假的 git＋`GIT_DIR` 讓真的 git 失敗 | `PC：` 4 條 |
| 範圍外第 3、4 件：逐頁清單與路由孤兒 | `controltest`（靜態）；`sweep`、`upgradecheck` 跑的時候再比一次 | `RT：` 5 條 |

**本質只能一次性的**（照實標，附理由與替代證明）

| 項目 | 為什麼只能一次 | 替代證明（常設的那一半） |
|---|---|---|
| 每一件的「修正前」（S1、S3–S7、第 1 點、範圍外四件） | 新舊兩版的歷史比對；舊版不會再變 | 新版的常設情境＋「把修正拿掉」的突變，等於每次重建舊行為再確認會紅 |
| F8 三個舊版的矩陣數字（`77b026d`／`3cf33ae`／`04b205e`） | 歷史事實 | `buildtest` 新版 54 格全擋；`BG：`、`WF：` 突變 |
| 判定改嚴時量到的「27 條多紅」 | 一次性的量測 | `checkmutations` 的 `alsoRed` 檢查＋`判定：` 突變 |
| 帶 `expect` 的 118 條重跑（`e128835`） | 某一個 commit 上的一次結果 | 突變整套（等四家一起）；`checkmutations` 每版查 `expect` 找不找得到 |
| F8 §5.14 真實資料、`build-stocks` 重跑（v0.7.24） | 打真的官方來源 | 每次真的跑 build 就是再驗一次；矩陣的「正常輸入要寫檔」對照格每版跑 |
| `livecheck` 對真證交所實跑 | 打真網路，照規矩不進 `npm test` | 判斷邏輯由 `controltest` 用錄音每版跑 |
| S8、S9 盤點本身 | 盤點是「當時的狀態」 | 盤點出的問題各自修了（F8、範圍外五件），修的部分有常設情境 |
| 情境 6 與範圍外第 3 件的等價突變分析 | 分析紀錄 | 真的會漏的那一種已登記成突變 |

**該補卻還沒補的**（另外列，這一輪沒有硬湊）

1. ~~`assertaudit`、`sweep`、`livecheck` 入口的「對照組沒過就停」~~ **已補**（2026-09-24，F10）：`entrygatetest` 在複本裡讓真的依賴失敗、從入口跑，見上面 F10 一節。
2. **232 條沒寫 `expect` 的突變**：它們只要紅就算，紅錯地方看不出來（§5.9）。Dispatch 2026-09-24 裁示：要補，每條寫明預期紅哪一組，判定改成「紅的正好是那一組」，可以分批。
   - **對照組已先做**：`checkmutations` 新增「故意把 expect 寫成另一組真的存在的標籤（那一組沒紅）→ 判成紅錯地方」。判定器的突變「判定忽略 expect」「細節行也當成失敗」都會連帶紅它，已用 `alsoRed` 列出。
   - **第一批已補 54 條、第二批 25 條**（見「突變補 expect：第一批」與「補充說明（十一）」一節）。**還剩 153 條**（全部 419 條，帶 `expect` 的 266 條）：142 條在瀏覽器類測試、11 條的突變檔在凍結區——照實留著。
3. ~~舊版量測用的腳本還在 session 暫存區~~ **已收進 `scripts/evidence/`**（2026-09-24，Dispatch 決定：「在證據檔寫明做法」和「可執行」不是同一件事）。收了 24 支：F8 三個舊版的矩陣數字（`f8old.sh`）、第 1 點的修正前（`p1ev.mjs`）、「27 條多紅」的逐條清單（`collate.mjs`）、S3–S7 與範圍外五件的修正前，以及它們共用的小工具。
   - 每支檔頭都有一行「本質一次性，保留供重做」。
   - 寫死的本機路徑都換成 `git rev-parse --show-toplevel` 與同目錄。
   - 當時用 `origin/main` 取舊版的，改成本檔寫的明確 commit（S3／S4 `84e3999`，S5–S7 `cb81089`），不然現在重做會取到新版。
   - 不在任何測試鏈裡；跳脫掃描與閘門孤兒掃描照樣掃它們，命中的逐條登記了理由。
   - **列進登記**（Dispatch 2026-09-24）：`scripts/evidencereg.mjs` 逐支登記用途、比哪兩個版本、數字在證據檔哪一段，以及「一次性、為什麼不用守」。檔頭那一行由登記產生。`doctest` 每版比對四件事：目錄＝登記（兩個方向）、檔頭＝登記、證據檔裡真的有那一段。合成的對照組四種問題都要報；`EV：` 4 條突變各拿掉一種，都紅在那一組對照。
   - **重做過一次**：`bash scripts/evidence/f8old.sh 77b026d` 得到「擋 0、碰巧擋下 33、沒擋 21」，跟上面 F8 表的原始數字一樣。其餘沒有逐支重跑。
4. **範圍外第 3 件修好後第一次跑時多紅一條「舊版的 Service Worker 已經接手這個頁面」**，重跑沒再出現：時序不穩，沒追。**Dispatch 裁示：下次再出現就追，當下留完整輸出與執行順序，不要只重跑。**

## 從 STATUS 搬過來的證據（P0 S1、S2；P1 S3、S4；P2 S5、S6、S8、S9——原文照錄）

> 2026-09-24 從 `docs/STATUS.md`「2026-09-24 檢查器盤點」一節搬過來；STATUS 原處換成一行指標。v8、v9 盤點表本身留在 STATUS。搬之前用程式確認每一段都逐字出現在本檔。

**S8：三支 build 的盤點（2026-09-24，SPEC_檢查器修補；只盤點、不改）**。全在暫存複本裡跑、不打網路：`build-calendar` 用 `--from` 餵本機檔；`build-dividends` 用 `node --import` 預載一個把 `fetch` 換成送合成回應的模組；`build-stocks` 用 `--cache` 放合成的六份來源，同時預載「任何網路請求都直接失敗」的 `fetch`。每一種都先跑對照（原樣或合成的正常輸入 → 回 0、寫出檔），再比輸出檔前後的雜湊。
（第一次跑時 18 種全部回 1，連對照也是——預載模組的路徑被編碼兩次、根本沒跑到被盤點的腳本；是對照組揭露的，修好後重跑。）
- `build-calendar`：空陣列、欄位改名（`Date`→`日期`）、不是陣列 → **停下、回 1、輸出檔沒變**，但三種講的理由都是「公告裡沒有 2026 年的休市日」（講不出「0 筆」或「欄位對不上」；欄位改名那種只多一行「N 筆日期解析不出來，已略過」的警告）；缺檔 → ENOENT、回 1。**只給前 5%（27 筆裡的 1 筆）→ 回 0、改寫了輸出檔**：2026 年變成交易日 260 天、平日休市 1 天（原本 243 天、18 天），沒有任何警告。
- `build-dividends`：空陣列、來源回 500、欄位改名 → **停下、回 1、輸出檔沒變、理由講得對**（「來源回的不是陣列或是空的」「來源回 500」「找不到「公司代號」」）。**只給前 5%（40 筆裡的 2 筆）→ 回 0、寫出 2 檔**；**代號全部不合格式、或現金股利欄全空 → 回 0、寫出「0 檔、0 筆」的輸出檔**，還印「每檔筆數 Infinity–-Infinity」。
- `build-stocks`：上市公司基本資料是空的、欄位改名、ISIN 上市表只剩一列 → 停下、回 1、輸出檔沒變，但**三種都是被「產業別只有 1 個，太少」這道門檻碰巧擋下**，講不出是哪一份來源空了或欄位變了；STOCK_DAY_ALL 是空的 → 「找不到標題列」、回 1；快取缺檔 → 去抓網路（實驗裡被擋）、回 1；上市公司基本資料不是 JSON → SyntaxError、回 1。**上櫃公司基本資料是空的 → 回 0、照樣寫檔**（6488 仍從 ISIN 上櫃表來，沒有東西發現少了一份來源）。
- 三支都是**直接寫最終檔**（沒有先寫暫存、全部成功才換上）；失敗在寫檔之前的，舊的輸出檔原封不動——但沒有任何東西告訴使用者「這份還是舊的」（`build-dividends` 的出表日期、App 的「可能已過期」提示除外）。

**S9 補測（2026-09-24，SPEC_檢查器修補）量到的新發現——範圍外，沒改，待 Dispatch 決定**：
- `upgradecheck` 的母體只有「換版後跑的是哪一版」與持股頁 → 定期定額那一段：新版的「股利」頁一開就拋錯，它照樣 17 項通過。
- `sweep` 的路由是寫死的 7 條：新註冊的路由（實測 `/zzq`）不會被巡檢、也不會被報出來；`/holdings/:code` 一直不在清單裡。
- `livecheck` 沒打 app 有用到的 `FMTQIK`（大盤成交資訊）端點；另見 S6 的實跑發現（日曆段崩潰）。
- `srcscan` 那一列原本寫「被 8 個測試檔用到」是沒數過的，實數是六支測試＋`affected.mjs`，已照實改。

   **2026-09-24 已修**（Dispatch 授權）：每支跑完數資料檔裡它的筆數，0 筆就講明「沒收到任何資料」、報告最後寫「不完整」並回傳 1；一支都沒跑到、或資料檔一筆都沒有，明講並回傳 1（以前是讀不到檔崩掉、意外停下）；資料檔某一行解析不了就停，不包 catch 當成 0 筆。驗收從真實入口（`node scripts/assertaudit.mjs <過濾>`）在暫存複本裡跑：兩支裡一支在寫出資料前崩掉——**修正前回傳 0、印「資料仍然收到了」；修正後回傳 1、講明「沒收到任何資料」、報告標「不完整」**（修正前的版本從 GitHub 追蹤分支取，先確認新舊雜湊不同、舊版含舊訊息——第一版在複本裡取 origin/main，取到的其實是新版）；正常兩支回傳 0；過濾到 0 支、登記的檔不見都回傳 1；測試紅了但資料有寫出來，報告完整、回傳 0。

   **2026-09-24 已修（S3，`SPEC_檢查器修補`）**：判斷邏輯抽到 `scripts/auditjudge.mjs`；`assertaudit` 開頭先跑四組對照（一定失敗的斷言、空母體、寫出資料前就崩、必過的乾淨測試），沒過就回傳 1、不產報告；同一組由 `scripts/controltest.mjs` 每版在 `npm test` 裡跑。突變清單加了以 `S3：` 開頭的四條。

   **2026-09-24 已修（S5）**：清單搬到 `scripts/auditjudge.mjs` 的 `AUDIT_TESTS`，補進 `taptest`、`controltest`、`gatescan`（孤兒檢查一接上就抓到這三支）；不收的寫在 `AUDIT_SKIP` 並寫理由（目前只有 `mutationtest`）。孤兒檢查（鏈上沒登記的、清單裡過期的、理由過期的）由 `controltest` 每版跑，`assertaudit` 開頭也跑、對不上就回傳 1。順帶：`taptest` 的探針會繼承 `SD_AUDIT`，把故意做的空母體寫進報告——探針改成清掉 `SD_AUDIT`。

   **2026-09-24 已修**（Dispatch 授權）：新增 `scripts/taptest.mjs`（進 `npm test` 鏈）——每種情境當場寫一支小探針、另開子程序跑，看回傳值與輸出（斷言一紅，呼叫它的測試就跟著紅，所以不能在同一個程序裡直接呼叫）；對照組是「母體非空、乾淨的斷言要放行」。突變清單加了 3 條以 `tap.mjs` 為對象的（`noneOf`、`everyOf` 的空母體檢查，`detects` 的「一定要有反例」），都帶 `expect`。

   **2026-09-24 已修（S4）**：版本解析與比對、快取、錯誤分類抽到 `scripts/sweepjudge.mjs`；`sweep` 開頭先用錄好的合成回應跑六組對照（sw.js 舊版、index.html 舊版、sw.js 讀不到版本、留著舊快取、不是新聞上游的錯誤、必過的全部一致），**不打網路**，沒過就回傳 1、不巡檢；同一組由 `controltest` 每版跑。突變清單加了以 `S4：` 開頭的六條。巡檢本體的斷言仍是 20 項。

   **2026-09-24 已修（S6）**：判斷抽到 `scripts/livejudge.mjs`（CORS、STOCK_DAY_ALL 格式、除權息標記、上櫃前提、TWT48U 格式、參考價公式、日曆對實際成交日、代號表、節流）；`livecheck` 開頭先用 `scripts/fixtures/` 裡錄好的回應跑九組對照，**不打證交所**，沒過就回傳 1；同一組由 `controltest` 每版跑。每組都是一對：好的錄音不能報，另外故意改壞一份錄音（標頭不見、2330 不見、X 標記不見、上櫃查得到、欄位改名、參考價差 0.01、少一個交易日、多一檔新上市、間隔 1.5 秒）必須報。每組各自包 try：判斷在改壞的錄音上拋錯，記成那一組沒過並講明例外，不讓整支崩掉（第一版沒包，一條突變讓 `controltest` 整支崩、看不出是哪一組）。錄音的限制：STOCK_DAY_ALL 只錄了 20 檔，對照組的筆數門檻用 10（正式巡檢仍是 1000）；「不存在的代號」沒有錄音，照上櫃那份的訊息造。

   **實跑發現（範圍外，沒改，待 Dispatch 決定）**：2026-09-24 在暫存複本對證交所實跑一次（重構後），前五段 23 項全過、0 項紅，接著在「data/calendar.json 與 TWSE 實際成交日一致」那段崩掉：`TypeError: cal.closed is not iterable`。原因：日曆改成多年格式後 `closed` 在 `years[年]` 底下，這段還讀頂層的 `cal.closed`（修正前的版本同一行就是這樣寫，不是 S6 造成的）。後果：日曆核對、代號表核對、節流檢查這三段**自從日曆改成多年格式就沒跑過**，`livecheck` 每次都以崩潰結束。同一次實跑還抓到 1 筆新的「預告 → 結果」配對樣本（公開市場資料，公式與公布值一致），因為是在暫存複本裡跑的，沒有寫回本 repo。

   **2026-09-24 已修（S5）**：`gatescan` 走訪整個 repo（跳過 `node_modules`、`.git`、`.logs`、`.private`；**含還沒 commit 的檔**），檔名帶 gate／precheck／piiscan／push、或不是註解的行裡呼叫了 git push、precheck、piiscan、gatepush 的，不在 `FILES` 就要在 `ORPHAN_SKIP` 寫理由（目前兩條：`gatescan.mjs` 自己、`mutationtest.mjs`），理由過期也報。對照組是當場造的小 repo。
