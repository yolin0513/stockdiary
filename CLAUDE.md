# StockDiary —— 給每一個新 Session 的必讀

**開工前先讀 `docs/STATUS.md`**，從開頭的「目前進行中／交接」讀起。那裡寫著現在做到哪、下一步是什麼、在等 Yolin 回覆哪些事。
接著讀「工作慣例」「開發流程慣例」「接手者最容易做錯的事」。規劃的唯一真相來源是 `docs/PLAN.md`。

## 常設規則（每一條都要遵守）

1. **全程繁體中文**，包括**思考與判斷的過程**，不只是最後的回覆。程式碼、變數名、檔名、指令、專有名詞、錯誤訊息原文維持原樣。
2. **嚴禁使用 `AskUserQuestion` 或任何互動式提示框／多選題工具。** Yolin 常從手機或另一台電腦遠端操作，提示框只渲染在本機，點不到會讓 Session 卡死。
   需要 Yolin 決定的事：**純文字**列出選項、標明你建議哪一個與理由，然後停下來等。
3. **授權邊界**：查詢、新增／搬移檔案、git（commit、push）、部署 GitHub Pages 與 Worker，**直接做，不必請示**。
   要先問的只有：刪除使用者資料、花錢、動到其他專案。
4. **跨專案唯讀**：`D:\Claude\App\TripQuest`、`D:\Claude\App\JLPT_App`、`D:\Claude\App\MealMate` 可以讀（抄慣例、對照同一種 bug），**不准改**。
5. **凍結區**：成本／損益對帳的數學（`js/avgcost.js`、`js/settle.js`、`js/money.js` 全部，以及 `docs/SPEC_全面優化.md` §0 逐函式列出的那些）在 Yolin 提供備份 JSON 與券商手續費折數之前**絕對不碰**，連過期註解也不改。非碰不可時停下來回報。
6. **不做任何投資建議、目標價、買賣建議**；試算器的報酬率與配息率只能由使用者自己填，不給預設值、參考值、歷史平均。**不碰券商帳密、下單、轉帳。**
7. **不接非官方或會被擋的資料來源**（Goodinfo、Yahoo 財經等）。
8. **測試紀律**：每條新斷言都要用突變證明會紅；不寫假斷言；檢查器要有對照組；母體要非空。
   平常只跑受影響的測試（`node scripts/mutationtest.mjs --changed`），全套只在 Yolin 叫「全面檢測」時跑。
9. **含 regex、反斜線、反引號、`$` 的程式碼一律用 Write／Edit 工具寫檔**，不要經過 shell heredoc；腳本裡的字串替換用 `split/join`，不用 `String.replace(字串, 字串)`（會把 `$$` 吃成 `$`）。
10. **回報時分清楚「實測驗證過的」與「推論的」**；做完一批就部署、回報版本號與逐項對照，然後**停下來等確認**再開始下一批。範圍外的發現先記下來回報，不要順手改。

## 共用慣例

四個 App 共用的工作慣例在 `docs/CONVENTIONS.md`。那是副本，主檔在統籌工作區，**不要在這裡改它**。
開工前把它跟 `docs/STATUS.md` 一起讀完，並在第一則回覆的**第一行**寫回執：`已讀共用慣例 vN（日期）`（N 與日期抄副本第一行）。
本檔與 `docs/STATUS.md` 的規則優先於共用慣例；兩邊衝突時照較嚴的做，並在回報裡指出來。

@docs/CONVENTIONS.md
